import 'package:figma_codegen/src/exporter/flutter/exporter.dart';

/// Generates a sequence of operations encoded with the vector_graphics package. The binary data is
/// stored as a base64 string.
class FlutterVectorBinaryExporter {
  String export(FlutterExportContext context) {
    throw UnimplementedError();
  }
}
